import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFE91E63); // Pink primary
  static const Color primaryLight = Color(0xFFF8BBD9);
  static const Color secondary = Color(0xFFFF4081);
  static const Color accent = Color(0xFFFFC1CC);
  
  // Fertility colors
  static const Color fertile = Color(0xFF66BB6A); // Green for fertile days
  static const Color ovulation = Color(0xFFFF7043); // Orange for ovulation
  static const Color period = Color(0xFFE53935); // Red for period
  static const Color predicted = Color(0xFFAB47BC); // Purple for predictions
  
  // UI colors
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Colors.white;
  static const Color error = Color(0xFFB00020);
  static const Color onPrimary = Colors.white;
  static const Color onBackground = Color(0xFF212121);
  static const Color onSurface = Color(0xFF757575);
}